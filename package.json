{
  "version": "0.5.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "bump": "node ./scripts/bump.js",
    "pack:windows": "powershell Compress-Archive -Path ./dist/* -DestinationPath dist.zip -Force",
    "pack:linux": "zip -r ./dist.zip ./dist"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.7.1",
    "@types/chrome": "^0.0.268",
    "typescript": "^7.0.2",
    "vite": "^8.1.4"
  },
  "engines": {
    "node": "24",
    "npm": "11"
  }
}
